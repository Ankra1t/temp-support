services:
  telegram-bot:
    build: .
    container_name: telegram-support-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./bot_database.db:/app/bot_database.db
    environment:
      - PYTHONUNBUFFERED=1
